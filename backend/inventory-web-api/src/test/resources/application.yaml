spring:
  datasource:
    url: jdbc:h2:mem:INVENTORY_DB
    username: sa
    password:
  jpa.hibernate:
    ddl-auto: none
  profiles:
    active: test

    
logging.level:
  org.springframework.security: ERROR